{
  "name": "bibliotouch-custom",
  "version": "1.0.0",
  "description": "",
  "main": "./server/index.js",
  "scripts": {
    "install": "npm --prefix ./public install",
    "build": "npm --prefix ./public run build",
    "start": "node ./server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  }
}
